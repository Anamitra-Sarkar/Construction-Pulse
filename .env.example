# MongoDB Atlas
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/quality_pulse?retryWrites=true&w=majority

# =============================================
# Firebase Configuration (IMPORTANT)
# =============================================
# SECURITY: Ensure all Firebase variables point to the SAME Firebase project
# The backend (FIREBASE_*) and frontend (NEXT_PUBLIC_FIREBASE_*) MUST use
# the same project ID to avoid authentication and data access issues.
#
# DO NOT reuse service account credentials across different projects.
# Each project should have its own dedicated Firebase project and credentials.

# Firebase Web Config (Client-side - must start with NEXT_PUBLIC_)
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=1:your-sender-id:web:your-app-id
NEXT_PUBLIC_FIREBASE_ENABLED=true

# Backend API URL
# Development: http://localhost:5000/api
# Production: https://your-backend-domain.com/api
# IMPORTANT: Must end with /api (the base API path)
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Firebase Admin Config (Server-side)
# SECURITY: Keep these values secret and never commit them to version control
# These should match the same Firebase project as the client-side config above
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Server Port
PORT=5000

# Admin Recovery Token (KEEP SECRET - use for disaster recovery only)
# Generate with: openssl rand -hex 32
ADMIN_RECOVERY_TOKEN=your-secure-random-token-here
